.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install namely

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv namely
    $ pip install namely